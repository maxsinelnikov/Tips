ifconfig    -  утилита, которая используется для назначения сетевого
	       адреса, изменения настроек сетевого адаптера и параметров
	       IP протокола.

------------------------------------------------------------------------
sudo        -  выполнение программ от лица суперпользователя
	       sudo -s

------------------------------------------------------------------------
nano        -  консольный  текстовый редактор

------------------------------------------------------------------------
apt-get     -  утилита выполняющая операции с установкой, обновлением,
	       индексацией программ, пакетов, и самой ОС.

------------------------------------------------------------------------
SSH	    -  защищенный сетевой протокол, позволяющий удаленно
	       управлять ОС. (Windows - PuTTy)

sudo apt-get install ssh

sudo service ssh start

------------------------------------------------------------------------
sudo apt-get update -y    -  поиск обновлений. Флаг  -y подавляет 
			     запросы на подтверждения к пользователю.

------------------------------------------------------------------------
sudo apt-get upgrade -y   -  установка обновлений
sudo apt-get update && apt-get upgrade -y   - в связке с update.

------------------------------------------------------------------------
sudo reboot

------------------------------------------------------------------------
sudo poweroff

------------------------------------------------------------------------ 
ls      -  просмотр содержимого каталога

------------------------------------------------------------------------
cd      -  переход в указанный каталог

------------------------------------------------------------------------
mkdir   -  создать папку

------------------------------------------------------------------------
chmod   -  изменение прав доступа к файлам и папкам:
	   755 - читать, запускать - all, редактировать - admin
	   777 - читать, запускать, редактировать - all

sudo chmod -R 755 /var/www     -  всем в папке

------------------------------------------------------------------------
chown    -  изменение владельца и группы для указанных файлов

sudo chown -R admin:admin /var/www

------------------------------------------------------------------------
sudo apt-get install mc    - установка Midnight Commander

------------------------------------------------------------------------
sudo nano /etc/network/interfaces   -  конфигурация сети

------------------------------------------------------------------------
sudo apt-get install apache2   - установка сервера Apache 
sudo apt-get install nginx	 или nginx

------------------------------------------------------------------------
DNS  - система автоматического сопоставления домена и IP-адреса

------------------------------------------------------------------------
/var/www/html/index.html

------------------------------------------------------------------------
ip				# используется для управления сетью
 
    link show			# смотрим сетевые интерфейсы

------------------------------------------------------------------------
which <program>			# поиск установленной программы

------------------------------------------------------------------------
cat <file>			# viewing file











 