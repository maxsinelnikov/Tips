#   ===    Ќастройки Git    ===    #

git config			# работа с настройками git

    --global			# глобальные настройки пользовател€

    --system			# системные настройки

    -l  --list			# вывод списка всех настроек

    user.name "Name"		# задать им€ пользовател€

    user.email "email"		# задать адрес почты

    core.editor "editor -opt"	# редактор по умолчанию

    color.ui true		#

    core.excludesfile <.file>	# глобальна€ настройка игнорировани€
				  файлов дл€ всех репозиториев



# ѕриглашение коммандной строки дл€ указани€ текущей ветки branch:
  export PS1='$(__git_ps1 "(%s)") > '

# ѕриглашение коммандной строки дл€ указани€ текущей ветки branch и
  директории:   export PS1='\W$(__git_ps1 "(%s)") > '

# nano .bash_profile добавить между if и fi

# ƒл€ настроек в Windows удобно использовать Launchy 